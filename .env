# ローカル開発用の設定。実際の秘密情報は含めない方針 (公開リポジトリのため)。
# 本番環境では下記の値を必ず上書き・再生成すること。

DATABASE_URL=postgresql://db_user:db_password@localhost:5432/sample

# Better Auth セッション署名鍵。
# このリポジトリにコミットされた値はローカル開発用のサンプル値であり、本番では絶対に使わないこと。
# 本番値は次のコマンドで生成: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
BETTER_AUTH_SECRET=mlktJO3X7gyaWMValShmSW+tabu5CzIa3uZoouUg9gM=
BETTER_AUTH_URL=http://localhost:3000
